# README

### Deploy ###

git clone https://github.com/epquick/jedi-test.git

cd jedi_test/deploy

docker-compose up -d

В результате на локальном хосте будет поднят сервер с описанным в задании сервисом.

Если зайти на /ip можно потестировать его и посмотреть _документацию_ к нему.

